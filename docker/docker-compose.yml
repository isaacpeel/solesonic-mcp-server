services:
  solesonic-mcp-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: solesonic-mcp-server
    network_mode: "host"
    env_file:
      - .env
    environment:
      - SERVER_PORT=8181
    volumes:
      - /var/log/solesonic-mcp-server:/var/log/solesonic-mcp-server
    restart: no