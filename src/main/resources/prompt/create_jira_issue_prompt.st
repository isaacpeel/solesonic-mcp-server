Invoke the tool `create_jira_issue` and pass the users input:

USER_INPUT:
{input}

### AVAILABLE TOOLS
The following tools are currently loaded in your environment. Use them precisely as named:

{available_tools}